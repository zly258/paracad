# Git Commit Guide

## Format

`type(scope): summary`

Examples:

- `feat(core): improve boolean fallback`
- `fix(view): remove duplicate preview objects`
- `refactor(graph): simplify transform execution`

## Notes

- Keep summary short and specific.
- Include test/build result in commit body when useful.
